#!/usr/bin/env bash

set -eu

ARCH_PACKAGES=(
    bash
    bash-completion
    ghostty
    micro
    shellcheck
    shfmt
    starship
    superfile
    wl-clipboard
)

AUR_PACKAGES=(
    blesh-git
    rcm
    visual-studio-code-bin
)

sudo pacman -S --needed "${ARCH_PACKAGES[@]}"
paru -S --needed "${AUR_PACKAGES[@]}"
